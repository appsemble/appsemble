parameters:
  fields:
    - value: { prop: date }
      label: { translate: date }
    - value: { prop: name }
      label: { string.format: { messageId: name, values: { locale: EN } } }
    - value: { prop: localName }
      label: { string.format: { messageId: name, values: { locale: NL } } }
events:
  listen:
    data: source
actions:
  onClick:
    type: link
    to: sourceUrl
# To better see how to use this block, look at the default 'Notes' app.
