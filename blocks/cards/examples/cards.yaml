parameters:
  defaultImage: logo
  card:
    title: Title
    subtitle: Subtitle
    content: Content
    image: { prop: picture }
actions:
  onClick:
    to: Person Details
    type: link
events:
  listen:
    data: data
