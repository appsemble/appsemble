parameters:
  item:
    header:
      icon: fontawesomeIcon
      title: { prop: name }
    content:
      image:
        file: photos
        rounded: true
        size: 64
      fields:
        - value: { prop: value }
actions:
  onClick:
    type: link
    to: Details page
events:
  listen:
    data: data
