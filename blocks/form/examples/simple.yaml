parameters:
  fields:
    - name: name
      type: string
      requirements:
        - required: true
    - name: birthdate
      type: date
      requirements:
        - required: true
    - name: avatar
      type: file
actions:
  onSubmit:
    type: resource.create
    resource: person
