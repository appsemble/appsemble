actions:
  onLoad:
    type: resource.query
    resource: report
events:
  emit:
    data: reports
  listen:
    refresh: refresh
# To better see how to use this block, look at the default 'Notes' app.
