import { defineMessages } from 'react-intl';

export const messages = defineMessages({
  requestButton: 'Reset',
  requestFailed: 'Request failed. Token may be invalid.',
  requestSuccess: 'Password successfully reset!',
  passwordLabel: 'Password',
});
