import { defineMessages } from 'react-intl';

export const messages = defineMessages({
  title: 'OAuth2',
  error: 'There was a problem loading the OAuth2 secrets.',
  loading: 'Loading OAuth2 secrets…',
  noSecrets: 'No OAuth2 secrets have been configured yet.',
  addNew: 'Add new secret',
});
