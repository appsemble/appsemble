import { defineMessages } from 'react-intl';

export const messages = defineMessages({
  title: 'OAuth2 Secrets',
  error: 'There was a problem loading the secrets. Please try reloading the page.',
  noSecrets: 'No OAuth2 secrets have been configured yet.',
  addNew: 'Add new secret',
});
