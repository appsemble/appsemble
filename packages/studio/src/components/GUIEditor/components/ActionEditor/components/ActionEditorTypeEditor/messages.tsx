import { defineMessages } from 'react-intl';

export default defineMessages({
  notSupported: `This action can’t be updated, because it is not available in the GUI editor yet.
    You can define this action with the manual editor.`,
});
