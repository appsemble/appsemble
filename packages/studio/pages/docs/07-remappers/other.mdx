import { RemapperRef } from './RemapperRef/index.js';
import { unsortedRemappers } from '../../../../utils/reference-schemas/remappers/index.js';

# Other

## Introduction

These are the remaining remappers. While they might not belong to a specific group, they can still
be very useful.

## Remappers

<RemapperRef remappers={unsortedRemappers} />
