import { RemapperRef } from './RemapperRef/index.js';
import { referenceSchemas } from '@appsemble/lang-sdk'

# OData

## Introduction

Appsemble uses [OData](https://www.odata.org/documentation/odata-version-2-0/overview/) under the
hood for resource queries. To make it easier to construct OData queries from within app definitions,
we have some special remappers.

## Remappers

<RemapperRef remappers={referenceSchemas.odataRemappers} />
