import { RemapperRef } from './RemapperRef/index.js';
import { referenceSchemas } from '@appsemble/lang-sdk'

# Numbers

## Introduction

Sometimes data is received in the form of a string, but is needed as a number. Number remappers help
in converting the data into the needed format.

## Remappers

<RemapperRef remappers={referenceSchemas.numberRemappers} />
