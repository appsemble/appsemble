import { RemapperRef } from './RemapperRef/index.js';
import { referenceSchemas } from '@appsemble/lang-sdk'

# Other

## Introduction

These are the remaining remappers. While they might not belong to a specific group, they can still
be very useful.

## Remappers

<RemapperRef remappers={referenceSchemas.unsortedRemappers} />
