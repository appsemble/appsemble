import { RemapperPage } from './Remapper/index.js';
import { numberRemappers } from '../../../../utils/reference-schemas/remappers/index.js';

# Numbers

## Introduction

Sometimes data is received in the form of a string, but is needed as a number. Number remappers help
in converting the data into the needed format.

## Remappers

<RemapperPage remappers={numberRemappers} />
