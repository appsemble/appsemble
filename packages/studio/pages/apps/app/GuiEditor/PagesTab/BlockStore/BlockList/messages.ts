import { defineMessages } from 'react-intl';

export const messages = defineMessages({
  error: 'Error',
  loading: 'Loading blocks…',
  emptyBlocks: 'No blocks can be displayed for your organisation',
  noBlocks: 'No blocks match your search',
});
