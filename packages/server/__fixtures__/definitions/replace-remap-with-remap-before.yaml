# Replace remap with remapBefore
name: test
description: test
defaultPage: test

pages:
  - name: test
    blocks:
      - type: action-button
        version: 0.29.0
        parameters:
          icon: arrow-right
        actions:
          onClick:
            remap: { object.from: { test: test } }
            type: noop
