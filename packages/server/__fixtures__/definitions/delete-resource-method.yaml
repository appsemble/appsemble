# Delete resource.method property
name: test
description: test
defaultPage: test

resources:
  test:
    roles: [$public]
    schema:
      additionalProperties: false
      type: object
      properties:
        test:
          type: string

pages:
  - name: test
    blocks:
      - type: action-button
        version: 0.29.0
        parameters:
          icon: arrow-right
        actions:
          onClick:
            type: resource.create
            method: post
            resource: test
