import { createQueryAppResourcesController } from '@appsemble/node-utils';

import { options } from '../../../../options/options.js';

export const queryAppResources = createQueryAppResourcesController(options);
