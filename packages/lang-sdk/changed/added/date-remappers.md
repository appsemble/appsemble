Add `date.startOf`, `date.endOf`, and `date.set` remappers.

`date.startOf` and `date.endOf` support the following units: `year`, `quarter`, `month`, `week`
(Monday start), and `weekSun` (Sunday start).
