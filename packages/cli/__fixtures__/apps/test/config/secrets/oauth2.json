[
  {
    "name": "test-oauth2-secret",
    "icon": "redhat",
    "scope": "email openid profile",
    "remapper": [{ "prop": "email" }],
    "authorizationUrl": "{{ TEST_OAUTH2_AUTHORIZATION_URL }}",
    "tokenUrl": "{{ TEST_OAUTH2_TOKEN_URL }}",
    "userInfoUrl": "{{ TEST_OAUTH2_USER_INFO_URL }}",
    "clientId": "{{ TEST_OAUTH2_CLIENT_ID }}",
    "clientSecret": "{{ TEST_OAUTH2_CLIENT_SECRET }}"
  }
]
