import { GetAppSubEntityParams } from '@appsemble/node-utils/server/types';
import { AppMessages } from '@appsemble/types';

export const getAppMessages = ({ app }: GetAppSubEntityParams): Promise<AppMessages[]> => {

}
