This is a simple app that demonstrates the email action in demo apps.

On `appsemble.app`, this demo app uses the production Mailpit deployment so submitted emails are
captured for inspection instead of being delivered through a real SMTP destination. Do not remove
production Mailpit without verifying that this demo app still works as intended.
