apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "appsemble.fullname" . }}-test-connection"
  labels:
    app.kubernetes.io/name: {{ include "appsemble.name" . }}
    helm.sh/chart: {{ include "appsemble.chart" . }}
    app.kubernetes.io/instance: {{ .Release.Name }}
    app.kubernetes.io/managed-by: {{ .Release.Service }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "appsemble.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
