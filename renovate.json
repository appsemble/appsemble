{
  "extends": ["config:best-practices"],
  "schedule": ["every weekday"],
  "prConcurrentLimit": 6,
  "rebaseWhen": "behind-base-branch",
  "minimumReleaseAge": "3 days",
  "customManagers": [
    {
      "description": "Track the 'mermaid svgs' job's mcr.microsoft.com/playwright image tag against the root @playwright/test npm version (the e2e job's image is pinned separately to @appsemble/e2e and kept in sync manually)",
      "customType": "regex",
      "managerFilePatterns": ["/^\\.gitlab-ci\\.yml$/"],
      "matchStrings": [
        "mermaid svgs:[\\s\\S]*?mcr\\.microsoft\\.com/playwright:v(?<currentValue>\\d+\\.\\d+\\.\\d+)-noble"
      ],
      "depNameTemplate": "@playwright/test",
      "datasourceTemplate": "npm"
    }
  ],
  "packageRules": [
    {
      "matchUpdateTypes": ["minor", "patch"],
      "automerge": true
    },
    {
      "description": "Bump the npm @playwright/test pins and the matching playwright Docker image tags in a single PR",
      "matchPackageNames": ["@playwright/test"],
      "groupName": "playwright"
    },
    {
      "description": "eslint-plugin-jsx-a11y has no released version supporting ESLint 10 yet; cap eslint below v10 until https://github.com/jsx-eslint/eslint-plugin-jsx-a11y/issues/1075 (tracked by PR https://github.com/jsx-eslint/eslint-plugin-jsx-a11y/pull/1081) ships a compatible release",
      "matchPackageNames": ["eslint", "@eslint/js"],
      "allowedVersions": "<10.0.0"
    },
    {
      "description": "@remcohaszing/eslint v13 requires migrating ~120 call sites across the repo to eslint-plugin-unicorn's stricter rules (e.g. no-array-sort, no-immediate-mutation), and v14 additionally requires ESLint 10, which is capped below v10 until https://github.com/jsx-eslint/eslint-plugin-jsx-a11y/issues/1075 (tracked by PR https://github.com/jsx-eslint/eslint-plugin-jsx-a11y/pull/1081) ships a compatible release; cap below v13 until the lint migration is done and that issue closes",
      "matchPackageNames": ["@remcohaszing/eslint"],
      "allowedVersions": "<13.0.0"
    }
  ]
}
